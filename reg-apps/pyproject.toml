[project]
name = "groupwise-niftyreg"
version = "0.1.0"
description = "Groupwise image registration using NiftyReg."
requires-python = ">=3.10"
dependencies = [
    "niftyregw>=0.1.3",
]

[project.scripts]
groupwise_niftyreg = "groupwise_niftyreg:app"
